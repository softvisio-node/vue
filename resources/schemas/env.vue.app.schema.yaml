type: object
properties:
  env:
    type: object
    properties:
      APP_TITLE: { type: string }
      APP_TITLE_ICON: { type: string }
      APP_API_URL: { type: string, format: uri-reference }
    required: [APP_TITLE]
required: [env]
